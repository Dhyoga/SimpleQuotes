nodemon --experimental-specifier-resolution=node --loader esm index.js",
nodemon index.js

Langkah menjalankan proyek
[ Terminal 1 ]
$ cd backend
$ docker-compose up -d
$ npm run dev

[ Terminal 2 ]
$ cd frontend
$ npm run dev